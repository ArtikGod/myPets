const API_BASE_URL = "/api";

const PAGE_SIZE = 20;

const ADD_BATCH_INTERVAL = 10000; 

const FETCH_INTERVAL = 1000; 

Object.freeze(API_BASE_URL);

Object.freeze(PAGE_SIZE);

Object.freeze(ADD_BATCH_INTERVAL);

Object.freeze(FETCH_INTERVAL);

export { API_BASE_URL, PAGE_SIZE, ADD_BATCH_INTERVAL, FETCH_INTERVAL };
